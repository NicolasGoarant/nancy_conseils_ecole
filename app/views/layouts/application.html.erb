<!-- BEGIN app/views/layouts/application.html.erb -->
<!DOCTYPE html>
<html>
<head>
  <title>Nancy - Conseils d'École</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= javascript_importmap_tags %>
</head>
<body>
  <header class="header-banner">
    <h1>Nancy - Conseils d'École</h1>
  </header>

  <%= yield %>

  <!-- Pas besoin de dupliquer ton script JS : il est déjà dans app/javascript/application.js -->
  
  <style>
    * {
      box-sizing: border-box;
    }

    body {
      font-family: 'Public Sans', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      background-color: #f8f9fa;
      margin: 0;
      padding: 0;
      padding-bottom: 60px;
    }

    .header-banner {
      background: linear-gradient(to right, #003366, #1a5ca5);
      color: white;
      padding: 1.5rem 2rem;
      display: flex;
      align-items: center;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .header-banner h1 {
      margin: 0;
      font-size: 1.8rem;
      font-weight: 700;
    }

    .login-page {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .login-container {
      max-width: 1200px;
      width: 90%;
      margin: 2rem auto;
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      overflow: hidden;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
      min-height: calc(100vh - 4rem);
      padding-bottom: 2rem;
    }

    .login-tabs {
      display: flex;
      border-bottom: 1px solid #eee;
    }

    .tab-button {
      padding: 1rem 1.5rem;
      background: none;
      border: none;
      font-size: 1rem;
      font-weight: 500;
      color: #666;
      cursor: pointer;
      position: relative;
    }

    .tab-button:after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 3px;
      background-color: #1a5ca5;
      transition: width 0.2s;
    }

    .tab-button.active {
      color: #1a5ca5;
      font-weight: 600;
    }

    .tab-button.active:after {
      width: 100%;
    }

    .tab-content {
      padding: 2rem;
      flex-grow: 1;
    }

    .login-form-container {
      max-width: 450px;
      margin: 0 auto;
    }

    .login-form-container h2 {
      margin-top: 0;
      margin-bottom: 1.5rem;
      color: #1a5ca5;
      text-align: center;
      font-weight: 600;
    }

    .field {
      margin-bottom: 1.5rem;
      position: relative;
    }

    .field-icon {
      position: absolute;
      left: 1rem;
      top: 2.5rem;
      color: #1a5ca5;
    }

    .login-form label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: #555;
    }

    .login-form input[type="text"],
    .login-form input[type="password"] {
      width: 100%;
      padding: 0.8rem 1rem 0.8rem 2.5rem;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 1rem;
    }

    .login-button {
      width: 100%;
      background-color: #1a5ca5;
      color: white;
      border: none;
      padding: 0.8rem 1rem;
      border-radius: 4px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
    }

    .login-button:hover {
      background-color: #003366;
    }

    .alert {
      padding: 0.8rem 1rem;
      background-color: #f8d7da;
      color: #721c24;
      border-radius: 4px;
      margin-bottom: 1.5rem;
    }

    .notice {
      padding: 0.8rem 1rem;
      background-color: #d4edda;
      color: #155724;
      border-radius: 4px;
      margin-bottom: 1.5rem;
    }

    .login-help {
      margin-top: 2rem;
      text-align: center;
      color: #666;
      font-size: 0.9rem;
    }

    .schools-header {
      margin-bottom: 1.5rem;
    }

    .search-filter-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    .search-container {
      position: relative;
      flex: 1;
      min-width: 250px;
    }

    .search-input {
      width: 100%;
      padding: 0.7rem 1rem 0.7rem 2.5rem;
      border: 1px solid #ddd;
      border-radius: 20px;
      font-size: 0.95rem;
    }

    .search-icon {
      position: absolute;
      left: 1rem;
      top: 50%;
      transform: translateY(-50%);
      color: #1a5ca5;
    }

    .filter-btn {
      padding: 0.5rem 1rem;
      border-radius: 20px;
      background-color: #eee;
      color: #555;
      font-size: 0.9rem;
      cursor: pointer;
    }

    .filter-btn.active {
      background-color: #1a5ca5;
      color: white;
    }

    .schools-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
      gap: 1.5rem;
      margin-top: 1rem;
      overflow-y: auto;
      padding-right: 0.5rem;
      padding-bottom: 2rem;
    }

    .school-card {
      display: flex;
      background-color: white;
      border: 1px solid #eee;
      border-radius: 8px;
      padding: 1rem;
      cursor: pointer;
    }

    .school-icon {
      width: 60px;
      height: 60px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.8rem;
      color: #1a5ca5;
      background-color: rgba(26,92,165,0.1);
      border-radius: 50%;
      margin-right: 1rem;
    }

    .school-details {
      flex: 1;
    }

    .school-name {
      margin: 0 0 0.25rem 0;
      font-size: 1.1rem;
      color: #1a5ca5;
    }

    .school-address {
      margin: 0;
      font-size: 0.9rem;
      color: #666;
    }

    @media (max-width: 768px) {
      .login-container {
        width: 95%;
        margin: 1rem auto;
        min-height: calc(100vh - 2rem);
        padding-bottom: 1.5rem;
      }

      .schools-grid {
        grid-template-columns: 1fr;
      }
    }

    /* Ajouté : styles du modal */
    .modal {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0,0,0,0.5);
      display: none;
      align-items: center;
      justify-content: center;
      z-index: 1000;
    }

    .modal-content {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      max-width: 500px;
      width: 90%;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
    }
  </style>
</body>
</html>
<!-- END app/views/layouts/application.html.erb -->




